# Security Baseline

Commercial release work must remove default credentials, plaintext sensitive fields and in-memory-only production sessions. State-changing APIs require authentication, authorization, CSRF protection and request limits. Customer data requires organization isolation, audited access and encrypted transport.

These controls are release blockers and will be implemented and tested in the security phase before production promotion.
