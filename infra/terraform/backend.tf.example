# Rename this file to backend.tf only after the S3 state bucket exists.
# Supply bucket/key/region through a local backend.hcl file so account-specific
# values are not committed to Git.
terraform {
  backend "s3" {
    encrypt      = true
    use_lockfile = true
  }
}
